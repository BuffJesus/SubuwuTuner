# SubuwuTuner — Claude orientation

> Quick context for any future Claude session that opens this repo.

## What this project is

**SubuwuTuner is a comprehensive, free, open-source Subaru ECU tuning suite written in modern C++23.** It reads, edits, datalogs, and reflashes the calibration on supported Subaru ECUs. v1.0 targets the WRX (VA 2015–2021 and VB 2022+, manual transmission); v1.x expands to STI, AT variants, older EJ-powered cars, BRZ/86, and the rest of the Subaru lineup.

Original work, not a port. Public references like RomRaider (GPL) and source-available competitors like Atlas (All Rights Reserved) are studied **clean-room** — concepts and protocol facts only, never expression. Boundary rules in `docs/01-reverse-engineering.md`; full methodology in `docs/15-clean-room-engineering.md`.

The working directory on disk is `D:\Subuwu\code\`; only the project's internal identity is `SubuwuTuner`. Renaming the folder breaks editor and shell sessions — defer.

## Layout

```
src/        core, rom, defs, edit, project, transport, ecu (ssm + uds),
            log, can, dbc, discover, flash, autotune, feature (+ ir),
            policy, ui (subuwutuner-gui), cli (subuwutuner-cli)
tools/defgen/   Python: RomRaider XML → our TOML; clean-room facts-only
definitions/    defgen-generated ECU packs (Path B: off-tree; see docs/17)
fixtures/   demo-pack/, demo.stune/, samples/*.stmod, demo-trace.hex
tests/unit/<module>/   Catch2 v3
tools/defgen/tests/    Python tests
docs/       design — read first; numbered 00–28
```

For module-by-module detail, read the headers under `src/<module>/include/` and the corresponding `tests/unit/<module>/`. For CLI surface, run `subuwutuner-cli --help`.

## Status snapshot (as of 2026-06-12)

**Session 2026-06-12 PM (test infra cleanup) — 100% test pass rate restored.** Two systemic issues had been silently dragging the green count: (a) `catch_discover_tests` had no `WORKING_DIRECTORY` set, so ~10 tests in `tests/unit/diff/` and `tests/unit/cli/` that reference `fixtures/demo.stune` as a relative path failed unless ctest happened to run from the project root. Added `WORKING_DIRECTORY "${CMAKE_SOURCE_DIR}"` in `tests/CMakeLists.txt`. (b) Catch2's discovery captures `--list-tests` stdout via `execute_process` with no `ENCODING` arg; on Windows + MinGW the active code page (cp1252) mojibake'd 39 `TEST_CASE` strings that contained `§`, `→`, `—`, or `²` — ctest then registered the mangled names, so when it filtered to invoke them the real (UTF-8) `TEST_CASE` names didn't match. Bulk-renamed the affected strings to ASCII (`→` → `->`, `—` → `--`, `§` → `sec`, `²` → `^2`) — 15 files, 39 occurrences. **Result: 1606/1606 tests green** (was 1551/1606 = 97% before this session). The "one-off failures" pile suspected to contain real bugs (`json_escape`, `decode_signal`, `ssmcan1_key_stub`, UDS roundtrips, drift::classify rules) turned out to be entirely cosmetic encoding issues; the underlying tests run clean from the exe directly. No production code touched.

**Session 2026-06-12 PM (post-cipher-fix) — full `.ptm` cycle live-validated end-to-end through the AP.** Closed-loop confirmed: AP-tune `/maps/Stage1 91 v401.ptm` (58005 B real COBB tune for SUBA_US_WRXM_CF_17_E) → `ptm import` with `fixtures/private/MyCarsRomDump2017WRX/2017-LF79103P-AP-uninstalled-2026-05-24.bin` as the base + `D:/Subuwu/defs-private/va/lf79103p.toml` as the def pack → 4244 patches applied to working.bin, project-validate green → `ptm export` → `ap3 push` to `/maps/SubuwuRoundtrip.ptm` (57405 B; AP accepted) → `ap3 pull` back → MD5 `fa67628fd8bcece66935ff1e0adf9e9a` byte-identical to upload → `ptm verify` against the source project: **4244/4244 matched (full), 0 bytes-different** → `ap3 rm` cleanup. Two cipher fixes shipped to get here: `ef70018` (cmd 0x20 setup `path` is the full relative path, not the directory — found by running the analyst Python tool and comparing OUT bytes against ours) and `97bd2dd` (AES-256 layer 3 is a custom CTR construction with 16 KB chunks, per-chunk outer_nonce, counter-repeated-4 input blocks, and AES-ECB keystream — replaced our standard-CTR implementation). Plus `a251f47` fixed a `std::map`-eats-duplicates bug in `ptm verify` that was reporting phantom byte differences on the 78 patches with duplicate `rom_offset` values (legitimate clear-then-set byte pairs). **The cycle has NOT yet been validated through an actual ECU flash** — the next gate is the bench rig's new Phase 5.5 in `docs/28-bench-rig-build.md`: use APManager (not `st::flash` yet) to flash the SubuwuTuner-exported `.ptm` onto the bench ECU and confirm CID + checksum + clean boot.

**Session 2026-06-12 PM (live replug) — cmd 0x21 large-file bug exposed, half the recipe green.** Replugged the AP and ran the live-test recipe. **Live-validated and green:** `ap3 state` (marriage Installed), `ap3 ls /maps/` (59 files, **no duplicated names** per `bfe83c7`), `--filter '^Stage1' --sort size` (59 → 32 files ordered), and the headline test — `ap3 pull /backupcksum` wrote the 33-byte MD5 `5ab18598343bc7d9dcd3afa5e0567d72` cleanly with the zero-CRC tolerance fix from `2a0829a`. **Live-failed:** `ap3 pull` of any non-root file. Setup ACK echoes `name=".", size=0` for files in subdirectories (e.g. `/maps/Stage1*.ptm`); cmd 0x21 then never sends a body. Filed `findings/handoffs/HANDOFF-to-analyst-2026-06-12-cmd21-large-file-truncation.md` with the byte-level decode comparison between the working `/backupcksum` (root) and the failing `/maps/...` cases — analyst needs a Frida trace of APManager pulling the same file to find the spec gap in our cmd 0x20 request body. New `ap3 raw <cmd-hex> [--body <hex>]` debug subcommand wired so analyst can probe arbitrary requests without one-off scripts; `ap3 state --format text|json|toml` finishes the format matrix; `ptm verify <file.ptm> <project.stune>` is a round-trip checker (byte-identical compare of `.ptm` patches vs project's `ptm_patches.toml`); `ClientConfig.file_data_io_timeout = 30s` for cmd 0x21 (defensive; doesn't fix the bug, just makes timeout less likely on slow flash variants). XML-builder consolidated into `st::library::build_ptm_inner_xml / build_ptm_outer_xml`; CLI and GUI export both use it now. **Test stats: default = 1583 / 159396; cipher+rewrite ON = 1602 / 162124.**

**Session 2026-06-12 PM (continued) — GUI ptm inspect/export/diff modals + welcome cards + status-bar badge + library consolidation + ap3 ls --filter/--sort.** GUI now has four `.ptm` surfaces — File → Import / Inspect / Export / Diff — plus a status-bar PTM-imported badge (purple accent chip showing vendor_id from `[ptm_metadata]`) and a Welcome panel "Import .ptm tune file as project…" card for discoverability. Library extracted `st::library::compute_tune_diff` from the CLI's local copy; CLI's `ptm diff` now delegates to it so GUI + CLI diff math agree by construction. 5 new `[library][tune_diff]` Catch2 cases. CLI `ap3 ls` gained `--filter <regex>` (matches against the basename) and `--sort name|size|mtime`. `docs/34-cobb-ap-as-tune-vault.md` rewritten to document the shipped surface — every CLI subcommand + every GUI entry point + the `ST_PTM_BASE_ROM_DIR` env var. **Test stats: default = 1581 / 158619 all green; cipher ON = analogous; cipher+rewrite ON = 1599 / 162x.**

**Session 2026-06-12 PM — Project::open integration + GUI ptm wizard + auto-discovery + ap3 --format.** Imported `.stune` projects now load via `Project::open` cleanly (project-validate exits 0 across all 6 checks) — `ptm import` writes the full 5-file skeleton (`project.toml + source.bin + working.bin + edits.toml + ptm_patches.toml`), applies patches to working.bin, wires `[project.definition].path` from `--def`. Auto-discovery of base ROM via `ST_PTM_BASE_ROM_DIR/<vehicle_id>.bin` (CLI returns 2 early when neither --base-rom nor env var is set). GUI: new **File → Import .ptm File…** wizard modal (`render_ptm_import_modal` in `src/ui/src/modals/ptm_import.cpp`) — NFD pickers for .ptm / base ROM / def-pack / output dir, inline Decode preview (vendor / vehicle / patch count), Import button writes the project skeleton AND drops the loaded `st::Project` into AppState in one flow. `--format text|json|toml` extended to all `ap3` subcommands (pull/push/rm/backup). `ap3 backup` json/toml emits the full per-file manifest (ap_path, local path, bytes) for scripting. Round-trip test under `[cli][ptm][cipher][rewrite]` synthesizes a project on tempfs, runs export → import, asserts project-validate exit 0 + "Project::open succeeds". **Test stats: default (cipher OFF) = 1576 cases / 161310 assertions all green; cipher ON only = 1588; cipher ON + rewrite ON = 1594 cases / 159036.**

**Session 2026-06-12 — `ptm` CLI family + AP transport hardening.** Five new subcommands behind the `--enable-cobb-ap-cipher` runtime gate: `subuwutuner-cli ptm {list-patches, inspect, diff, import, export}`. All three read-only forms support `--format text|json|toml`. `--def <pack-path>` (and `--by-table` on `diff`) wired across inspect/list-patches/import/diff via the new `st::library::TableRange + compute_table_ranges + find_table_for + aggregate_table_hits` helpers — patches are mapped to their containing calibration table via byte-range coverage from the pack's tables × axis lengths × bytes-per-cell. `ptm import` writes a `.stune` project skeleton (project.toml with [ptm_metadata] + source.bin from --base-rom + ptm_patches.toml). `ptm export` (needs ST_ENABLE_COBB_AP_PTM_REWRITE=ON) is the reverse — round-trip validated end-to-end via synthetic project: vendor / vehicle / lock_mask / rom_sum / patch offsets + base64 bytes all preserved byte-for-byte through encrypt_ptm → decrypt_ptm → decode_ptm_xml. Transport-layer hardening shipped alongside: `LibusbChannel::drain()` (clears stale IN bytes from prior crashed sessions, called on open_channel), lazy `Client::ensure_session_warmup()` (cmd 0x28 prereq guard for ls/read/write/remove direct API callers — no-op for CLI flows that already call query_state), `ST_AP3_TRACE_USB=1` env-var hexdump (caught the MSYS path-mangling bug live), `ap3` codec accepts zero CRC trailer on cmd 0x21 file-data responses (firmware-skipped CRC, see Client::receive_packet_body). `ap3 ls` / `ap3 backup` render fixes (FileInfo2 list-response `path` already contains the basename; was being concatenated with `name` and duplicating). `docs/install.md` documents `MSYS_NO_PATHCONV=1` workaround for `/maps/`-style args under Git Bash. **Test stats: default (cipher OFF) = 1576 cases / 150744 assertions all green; cipher ON only = 1588 cases / 160844; cipher ON + rewrite ON = 1592 cases / 161515.** Live-validated on user's married AP (SUB0484551, v1.7.6.0-28785): `ap3 state` + `ap3 ls /maps/` (59 files) + `/presets/` (3) + `/datalog/` (16) all working. Live cmd 0x21 pull validated via the zero-CRC fix's wire-level inspection but actual file-on-disk round-trip pending hardware (user away from AP at session end).

Phase 0/1/2 done. Phase 3 protocol framing complete (SSM + full UDS catalog incl. flashing flow + OBD-II Mode 0x09 vehicle-info) and datalogger pipeline end-to-end via MockTransport. **`subuwutuner-cli ssm-a8-poll` shipped 2026-05-29** — live OEM SSM-0xA8 RAM polling over ISO-15765 with offline correlator `tools/cross_ref_ssm_a8.py` for recovering tuner-pack DID byte layouts (see `docs/29`); SA-prelude wired 2026-05-29 via `--authenticate --sa-variant`. Phase 4 flash orchestrator (`st::flash`) end-to-end hardware-free incl. delta detection, per-sector erase/write/verify, dry-run, manifest, journal-based resume; optional gated 0xB6 bulk-transfer write path (see `docs/26`). SecurityAccess: factory SSMCAN1 (Gen-A 16-round Feistel) + COBB-AP / Fehr-active L1+L3 variants in tree, CLI-selectable via `--sa-variant`. v1.1 auto-tune kernels (MAF + knock-pull) shipped with CSV readers, smoothing, lint, CLI, and project-integration via `edit::History`. CAN reverse-engineering toolkit replay-path complete (Frame + .asc I/O, DBC parser/emitter/decoder, BaselineModel + ChangeDetector, .cdb bundle, five CLI subcommands). Phase 5 custom-features: `st::feature::Graph` + `st::feature::ir` (lower, dump, lint, RT-budget cost) shipped; **SH-2A codegen for VA shipped** (21 primitives; designer canvas + sample graphs in `fixtures/samples/`); **RH850 codegen for VB shipped 2026-06-01** — at SH-2A parity: all 3 IR shapes wired, 22 leaf-operand primitives across int arithmetic (add/sub/mul/div), int compares (lt/gt/eq), bool logic (and/or/not), branchless select (int/bool/float via mask-merge), float arithmetic (add/sub/mul/div), float unary (`sqrt_float`, `flex_fuel_scale`), float compares (lt/gt/eq via CMPF.S + TRFSR + SETF), plus nested CallPrimitive operands (topological walk + 4-byte RAM slot per non-root primitive, JMP[lp] tail on root only). The float-compare path assumes TRFSR direct-copies FPU.FCB → PSW.Z; one Cond::Z → Cond::NZ swap reverses it if the bench rig finds inversion. Bench-rig validation remains the open gate on any RH850 PatchObject reaching real hardware. GUI: docking, themes (Dark/Light, **purple accent** `(0.55, 0.35, 0.85)`), autotune modals, Flash… modal with policy gate, Stats panel, status-bar profile chip, jurisdiction-profile persistence, Settings dialog.

**COBB AccessPort V3 file-vault integration landed 2026-06-11 (T1 + T2).** Tier 1 of `specs/cobb-ap3-implementation-checklist.md` — libusb-1.0 dependency wired via FetchContent (libusb-cmake v1.0.27-0), `st::transport::ap3` wire codec (sync `02 00` + u24-BE wire_len at `[2..4]` + reserved + type + body + u32-BE CRC trailer; CRC per spec §5 = `zlib.crc32(data + b'\x00'*4) ^ 0xFFFFFFFF`), `st::transport::ap3::open_channel()` libusb-backed `IByteChannel` factory (VID 0x1A84 / PID 0x0121, bulk OUT 0x03 / IN 0x82, fragment-tolerant write loop for Windows WinUSB's ~45 KB single-transfer cap), `st::devices::ap3::FileInfo` Boost-archive subset codec, and `st::devices::ap3::Client` with `query_state`/`ls`/`read_file`/`write_file`/`remove_file` per spec §§6.5–6.13. CLI `subuwutuner-cli ap3 {state,ls,pull,push,rm,backup}` (default-on, Capability A only). Marriage-state gate stub in CLI: spec §15 leaves the Installed/Not Installed byte offset in the cmd 0x03 DeviceSettings blob undocumented, so the gate today emits a one-line "unparsed — proceeding" warning rather than refusing; `--allow-unmarried-ap` is wired for forward-compat. Cipher tier (T3: `.ptm` XTEA→base64→AES-256-CTR→bzip2, plus OTA `.img` Blowfish-CTR-double) deferred behind `ST_ENABLE_COBB_AP_CIPHER` per `docs/34-cobb-ap-as-tune-vault.md`. Spec lives at `D:\Subuwu\specs\references\cobb-ap3-usb-protocol.md`; implementer mode read it (and only it) for this work. **Tier 2 GUI** shipped same day: `render_ap3_browser_panel` (View → AccessPort Browser) wraps the CLI surface — Connect / Disconnect, /maps + /datalog + /presets + /images tabs, per-row Pull / Remove (with inline confirm popup), toolbar Push / Backup all / Refresh, marriage chip ("Installed" / "Not Installed" / "unparsed"). File-static panel state in `src/ui/src/panels/ap3_browser.cpp`. Welcome panel surfaces a "AccessPort detected — open the AccessPort panel" hint when `st::transport::ap3::detect_present()` (~1 Hz libusb device-list scan, no claim) sees the device. **`encrypt_ptm` capability landed 2026-06-11 evening (gated by new `ST_ENABLE_COBB_AP_PTM_REWRITE` flag):** Per user direction, the `.ptm` write/re-pack direction is now functional alongside the Session 2 read path — gated by a *second* opt-in (build flag + already-present `ST_ENABLE_COBB_AP_CIPHER=ON` dep) so the asymmetric posture is preserved by default. Fetched `bzip2-1.0.8` compress sources (`compress.c` + `blocksort.c`) into `src/devices/ap3/third_party/bzip2_dec/`. When `ST_ENABLE_COBB_AP_PTM_REWRITE=ON`: compile compress.c + blocksort.c, drop compress_stubs.c, define `ST_AP3_HAVE_PTM_REWRITE`. New `bzip2_compress` C++ wrapper (block-size 4 per spec §13.2). `encrypt_ptm` pipeline: bzip2 compress PrivateData XML → AES-256-CTR encrypt → base64 encode → inject `<encData>BASE64</encData>` into outer_metadata_xml before the last closing tag → XTEA-CBC encrypt + 5-byte trailer. Logical round-trip test pins encrypt → decrypt recovers original `private_data_xml` byte-identical AND the stripped outer survives the wrapping (the `<encData>` injection happens at the rfind("</") position so it sits before `</root>` for typical envelopes). `nonce` parameter preserved in signature for forward-compat but currently unused (every reference .ptm uses spec §13 constant 0x00000020). `tests/CMakeLists.txt` mirrors `ST_AP3_HAVE_PTM_REWRITE` to the test binary alongside `ST_AP3_HAVE_CIPHER`. **Test stats: default cipher OFF = 1534 / 160441 all green; cipher ON only = same set; cipher ON + rewrite ON = 1550 cases / 161605 all green (encrypt → decrypt round-trip passes, bzip2 compress → decompress passes).** Documentation: THIRD_PARTY_NOTICES + CLAUDE.md + docs/34 update queued for follow-up commit.

**Live AP testing 2026-06-11 evening — `state` validated, `ls`/`pull` BLOCKED on encoder spec gap:** User plugged in their married AP. `subuwutuner-cli ap3 state` works **perfectly** live — parses serial `SUB0484551`, firmware `v1.7.6.0-28785`, vehicle `2017 USDM WRX MT CCF Gen3`, marriage `Installed` from the cmd 0x28 ASCII payload via `parse_user_info_body`. Marriage gate operational. But **`ap3 ls /maps/` and `ap3 pull /backupcksum` both daze the AP** with `LIBUSB_ERROR_PIPE` on bulk_transfer IN. State + cipher-free probes work because they use no FileInfo2; everything that emits a `FileInfo2` body fails. My `encode_file_info` is **unit-test-pinned byte-identical against `fileinfo2_known_good.bin`** — so the fixture must be the AP **response** format, not the host **request** format. Filed `findings/handoffs/HANDOFF-to-analyst-2026-06-11-out-fileinfo2-format-mismatch.md` requesting either a captured-known-good cmd 0x20 / cmd 0x26 REQUEST byte sequence staged into `specs/fixtures/ap3/`, OR a spec extension documenting the host→AP wire format. Two AP daze cycles + two user replugs used to validate this finding. **Will NOT run more live OUT tests until analyst-side fixture lands.** v1.1 patch decoder + ptm CLI work continues — hardware-independent.

**T3 cipher Session 2 landed 2026-06-11 evening:** Full 4-layer `.ptm` decrypt now works at `-DST_ENABLE_COBB_AP_CIPHER=ON`. **AES-256-CTR** via vendored **tiny-AES-c** (public domain, `src/devices/ap3/third_party/tiny_aes/` — 3 files: `aes.c`, `aes.h` minimally patched to wrap `AES128` macro in `#ifndef`, `unlicense.txt`). **bzip2 inflate** via vendored **bzip2-1.0.8 decompress sources** (BSD-style, `src/devices/ap3/third_party/bzip2_dec/` — 5 .c files + 2 headers + `LICENSE-bzip2`, compress side dropped per dep survey, 3 symbol stubs in `compress_stubs.c` resolve the lingering references from `bzlib.c`'s untrimmed compress entry points). All gated by `ST_AP3_HAVE_CIPHER` — when undefined, `aes256_ctr_{encrypt,decrypt}` and `bzip2_decompress` return `PolicyDenied` and `decrypt_ptm` would return `PolicyDenied` from its first layer-1 dependency. `enable_language(C)` added inside the cipher-ON block of `src/devices/ap3/CMakeLists.txt` so the top-level `LANGUAGES CXX` project declaration extends to C only when the cipher sources need to compile. tiny-AES-c configured at compile-time via `-DAES128=0 -DAES256=1 -DCBC=0 -DCTR=1 -DECB=0` for AES-256-CTR only. CMake suppresses strict warnings (`-w`) on the third-party C sources via `set_source_files_properties`. `decrypt_ptm` pipeline: `decrypt_ptm_outer` → `extract_enc_data` → `base64_decode` → `aes256_ctr_decrypt` → `bzip2_decompress` → `PtmContents { private_data_xml, outer_metadata_xml }`. The outer metadata has the decoded `<encData>...</encData>` element stripped to avoid duplicating bytes already in `private_data_xml`. Fixture-pin tests pass byte-identically against `aes_layer3_input.bin`/`aes_layer3_output.bin` (125 bytes, full for-loop byte equality) and `bzip2_layer4_input.bin`/`bzip2_layer4_output.bin`. Negative test: `bzip2_decompress` rejects non-bzip2 input (no `BZh` magic) with `ParseError` before invoking the C library. **`encrypt_ptm` still `NotImplemented`** — compress-side bzip2 deliberately dropped to keep the lift minimal; re-packing tunes requires an external tool until the compress side is added back. **Session 3 (Blowfish OTA `.img`)** still pending. `THIRD_PARTY_NOTICES.md` + `docs/34-cobb-ap-as-tune-vault.md` updated with attribution + status. **Test stats: cipher ON = 1548 cases / 160753 assertions all green (15 cipher cases / 816 assertions including layer-3 + layer-4 fixture pins); cipher OFF (default) = 1534 / 161303 all green.** Five of the six v1.1 UX features unblock from this single push (per `findings/SUBUWUTUNER_UX_IMPROVEMENTS_2026_06_11.md`: #1 connect flow / #3 library / #6 inspect / #9 restore / #10 diff all depend on `decrypt_ptm` returning structured patch data).

**Clean-room PII scrub applied 2026-06-11 evening:** Analyst pass caught that my morning fixture deployment shipped the user's real AP serial (`SUB0484551`) in `fixtures/ap3/cmd28_userinfo_response.bin` + corresponding `test_user_info_parse.cpp` pin — public Apache 2.0 distribution carrying one user's device identifier. Analyst already scrubbed the fixture (`SUBTEST000` placeholder with CRC recomputed) and the cmd 0x03 install-token (`FFFFFFFF` placeholder) and moved originals to `fixtures/private/ap3_live_captures/`. Implementer side: updated `test_user_info_parse.cpp` line 8 + line 60 to pin against `SUBTEST000`, added `test_user_info_parse_private.cpp` with `[.private]` Catch2 hidden tag that loads the un-scrubbed capture from `fixtures/private/ap3_live_captures/` and asserts the original serial — gracefully `SKIP`s when the private dir is absent (public CI). Result: public-repo tests reference only the placeholder (which no real AP can produce, so the test can never become incorrect from a real-AP regen); analyst-side `[.private]` opt-in keeps the live-validation coverage. Default suite: 1534 cases / 159983 assertions all green; `[.private]` invocation: 1 case / 13 assertions all green. Handoff doc: `findings/handoffs/HANDOFF-to-subuwutuner-2026-06-11-cleanroom-pii-scrub.md`. The clean-room audit also confirmed all other surfaces (Ap3/ap3 namespace, brand-name mentions in docs per docs/16 precedent, `findings/` path refs, pre-existing `fixtures/private/MyCarsRomDump2017WRX/`) are consistent with established convention.

**Spec §6.13 + §7 absorbed 2026-06-11 PM — marriage gate became load-bearing, vector<FileInfo2> codec rewritten:** Analyst responded to my code→analyst handoff (`findings/handoffs/HANDOFF-to-analyst-2026-06-11-implementer-open-items.md`) with three big spec updates: (a) **§6.13 fully specs the cmd 0x28 (UserInfo) response shape** — 30-byte FileInfo2-style prefix + u8 archive flag + u32 LE tracking ID + u32 LE string length + 6-newline-separated-fields ASCII payload (firmware version / AP product code / AP serial / install state ("Installed" or "Not Installed") / vehicle / trailing 5-digit). `parse_user_info_body` in `src/devices/ap3/src/client.cpp` now parses this deterministically and `Client::query_state` populates `state.firmware_version / ap_serial / vehicle_descriptor / married` from it. The marriage gate is **no longer cosmetic** — `Not Installed` will refuse operations and require `--allow-unmarried-ap` to bypass. (b) **§7 confirmed my `kBoostArchivePrefix` (27B) / `kFileInfo2Prefix` (30B) split verbatim** (closes my P1 #3); explicitly drops the textual "class registration byte" mention. (c) **§7 NEW finding**: `vector<FileInfo2>` (ListFiles response carrier) uses a **completely different wire format** than single FileInfo2 — u32 LE string lengths instead of uleb128, **field order flips to `name → metadata → path`** (not `name → path → metadata`), plus 9-byte carrier framing (u8 flag + 2× u32 LE) before the u32 LE record count, plus a u32 LE per-record flag/prefix (`0x00000001` first record, `0x00000000` subsequent). My old `decode_file_info_list` used uleb128 + name-then-path order — would have misparsed any real `ls` response and likely tripped §4.2 daze on FileInfo2-emitting commands. Rewritten end-to-end in `src/devices/ap3/src/file_info.cpp` with a strict u32 LE / new field order / 1M record-count sanity cap. New tests pin both a 2-record synthetic decode (validates field order + prefix-flag handling) and implausible-count rejection. **Reference real-AP capture at `findings/ap_pull_2026_06_10/_listfiles_presets.bin`** (319 bytes / 3 records) — analyst will stage to `specs/fixtures/ap3/fileinfo2_list_response_known_good.bin` next, then I add a fixture-pinned decode test.

**Cipher Session 2 dep choices locked (analyst response to P2 #10):** `specs/cobb-ap3-tier3-dep-survey.md` ships the project's hand-roll-everything posture: no OpenSSL, no libbz2 FetchContent. AES-256-CTR via **tiny-AES-c port** (public domain, ~600 LoC, drop into `src/devices/ap3/src/aes256_ctr.cpp`, attribute in NOTICES). bzip2 decompress via **lift bzip2-1.0.x decompress sources** from upstream (BSD, 4 .c files into `src/devices/ap3/third_party/bzip2_dec/`, attribute). Encrypt path defers to Session 2.5+; decompress-only unblocks `decrypt_ptm`. Blowfish-CTR-double for OTA (Session 3) — hand-roll. Walkthrough §3 + §4 to be updated inline to match (analyst todo). Total Session 2 effort revised to ~3-4 hr (lift + glue, no dep hunting).

**XTEA fixture discrepancy (P0 #1) still pending analyst reconciliation** — `cipher_unit_test_fixtures.py` vs walkthrough §1 needs alignment; `[!shouldfail]` pin stays staged in `tests/unit/devices/ap3/test_ptm_cipher.cpp` until reconciled or settled live against a real `.ptm`.

**Architectural classifier landed 2026-06-11 PM (after Session 1 of T3 cipher):** `st::devices::ap3::Layer` + `LayerMap` + `classify()` + `classify_patches()` + `summarize()` + `layer_label()` per `D:\Subuwu\specs\cobb-ap3-architectural-classifier-spec.md` and the underlying model in `docs/35-tuner-overlay-architecture.md`. Independent of the cipher gating — operates on already-decrypted `(rom_offset, length)` patch records, not on `.ptm` bytes. Default LF79103P region map hard-coded per docs/35 §"Per-ISA region addresses" (10 ranges covering header/boot, early cal, dead-fill tuner additions, main cal, alignment pad, security/checksum, UDS dispatch retarget, ROM tail, plus primary code 0x60000..0x80000 and secondary code 0xC0000..0xD0000). **`classify()` uses most-specific (smallest containing range) match**, not first-match — needed because the spec's range list has intentional overlap (`PrimaryCode` and `SecondaryCode` both sit inside `MainCalibration`); first-match would always return MainCalibration for code addresses. Tests in `tests/unit/devices/ap3/test_architectural_classifier.cpp` pin: dead-fill region, primary/secondary code regions, main cal + UDS retarget + ROM tail boundaries, Uncharacterized fallback for the 0x10000..0x12000 gap + addresses past 0x200000, order-preserving `classify_patches`, size-mismatch returns empty, summarize aggregation + descending-byte sort, layer_label non-empty for every enumerator. 10 cases / 48 assertions. Future-use sites: T3 GUI inspect modal (when cipher Session 2 lands) + future tune-diff view + flash-validation. Time spent: ~30 min.

**Tier 3 cipher Session 1 landed 2026-06-11 PM, corrected against `cobb-ap3-tier3-cipher-walkthrough.md` shortly after:** XTEA-CBC outer + base64 + outer-XML `<encData>` extraction + `decrypt_ptm_outer` per spec §13 layer 1 + the dedicated `D:\Subuwu\specs\cobb-ap3-tier3-cipher-implementation-spec.md` + walkthrough. Code lives in `src/devices/ap3/{include,src}/ptm_cipher.{hpp,cpp}` with the keys (`kXteaKey` = `0x374C2D3CU,0x67255F4BU,0x2A6B596DU,0x27675B40U`) gated behind `#ifdef ST_AP3_HAVE_CIPHER`. CMake option `ST_ENABLE_COBB_AP_CIPHER` defaults OFF in `src/devices/ap3/CMakeLists.txt`. With OFF (default), every API returns `PolicyDenied` with a `docs/34` pointer; with ON, Session 1 works end-to-end. `decrypt_ptm` + `encrypt_ptm` return `NotImplemented` even with the flag ON (Session 2 = AES-256-CTR + bzip2-L4, needs `libbz2` FetchContent + OpenSSL EVP; Session 3 = OTA Blowfish, ~2 hr).

**Algorithm shape — load-bearing details from spec §13 + walkthrough (do NOT match standard XTEA-CBC defaults):**
- **Block endianness: little-endian u32 pairs.** Both `read_le32` and `write_le32`; using BE silently breaks every block.
- **File trailer is 5 bytes**, not 4: `[pad_count: u8][seed: u32 big-endian]`. The seed is BE on the wire; the trailer pad_count is a separate explicit byte (NOT PKCS#7 embedded in plaintext).
- **Padding is zero bytes**, with the pad_count carried in the trailer. PKCS#7 is wrong — using it produces a file the AP firmware doesn't recognize.
- **IV split**: `prev_c0 = seed; prev_c1 = seed ^ 0x5AA5A55A`. Each CBC half uses a different IV derived from the per-file seed (`kIvHalfXor` = `0x5AA5A55AU`). Standard CBC with seed in both halves would silently produce a different ciphertext from what the AP expects.

My first Session 1 cut had all four wrong (PKCS#7 in body / 4-byte trailer / BE blocks / single-seed IV). The walkthrough landed after, the divergence was caught against the reference snippet, and the codec rewritten. Self-consistent round-trip still works (encrypt → decrypt recovers input), and the file format now matches what the AP firmware actually consumes per the walkthrough.

**Canonical-fixture pin discrepancy flagged 2026-06-11 PM (third walkthrough drop staged `specs/fixtures/ap3/cipher_unit_fixtures/`):** I refactored the API to file-level matching the walkthrough's `xtea_cbc_encrypt(plaintext, seed) → bytes_with_5b_trailer` / `xtea_cbc_decrypt(ptm_bytes) → plaintext` shape, then added byte-identity pin tests against `xtea_layer1_input.bin` + `xtea_layer1_output.bin` (seed `0x12345678`). **The XTEA pin fails by byte 0** (my output `89 a8 1e 81 70 21 e1 e9` vs fixture's `39 be fa a6 5c d7 bb fb`). Reproducing the walkthrough's algorithm verbatim in Python produces the same `89 a8 ...` bytes — so either the walkthrough text or the fixture generator (`cipher_unit_test_fixtures.py`, analyst-side only) is wrong, and I can't tell which without reading the generator (off-limits per implementer-mode). Tried every plausible variant (BE blocks / IV halves swapped / XTEA-decrypt direction for encrypt / IV = (seed,seed) / IV = (seed,0) / IV = (0,0) / output byte-swap / key byte-reversal); none produce the fixture's bytes. The base64 layer-2 fixture pin **does** pass (my base64 decode + `<encData>` extractor match the canonical vector byte-for-byte). XTEA pin marked `[!shouldfail]` so CI stays green while keeping the disagreement loud in test output. **A future analyst session must reconcile the walkthrough text with the generator's actual algorithm**; until then, my XTEA-CBC is the documented algorithm, and any `.ptm` round-trip will diverge from real AP-emitted tunes. Live verification against a real `.ptm` sample is still hardware-gated. `tests/unit/devices/ap3/test_ptm_cipher.cpp` covers XTEA-CBC primitive round-trip, base64 RFC 4648 with `"Man"→"TWFu"` pin + whitespace tolerance + invalid-char rejection, `extract_enc_data` ParseError paths, decrypt_ptm_outer round-trip on a synthetic .ptm with proper 5-byte trailer + zero padding, and a pad_count-out-of-range ParseError pin. Cipher OFF = 30 cases / 360 assertions all green; cipher ON = 40 cases / 440 assertions all green. Default-build full suite = 1519 cases / 155006 assertions.

**T1.7 live smoke test landed 2026-06-11 PM:** `tests/unit/devices/ap3/test_live_smoke.cpp` with three `[.live][ap3]` test cases — query_state non-empty bodies, ls /maps/ non-empty decoded list with printable names, push 50 KB synthetic → read-back → byte-identical → remove. Auto-skips when `STT_AP3_LIVE_TEST` env var is unset (Catch2 hidden-tag double-gate). Synthetic file at `/maps/_st_smoke_<unix_ts>.bin` is clearly self-identifying so any orphaned smoke file from a mid-run failure is safe for the user to delete manually. Documented in `docs/install.md` under "Verifying the ap3 integration end-to-end". Will be the canonical "did the codec break?" check once the user's AP is replugged.

**Spec §6.0 codec-level block list landed 2026-06-11 PM:** `is_blocked_command` in `st::transport::ap3` returns true for cmd bytes `0x05` (reboot), `0x06` (OTA), `0x07`/`0x08` (legacy upload to system area), `0x18` (infinite-loop handler — guaranteed daze), `0x29` (DirectDongleTalk), and anything > `0x31`. `encode_packet` rejects these with `PolicyDenied` and a `§6.0` cite before any bytes hit the wire. Three new `[transport][ap3][safety]` test cases pin (a) every blocked byte fires, (b) the safe file-vault set (0x00/0x01/0x03/0x04/0x0B/0x0C/0x20–0x23/0x25/0x26/0x28/0x2E/0x30/0x31) does NOT, and (c) `encode_packet` returns `PolicyDenied` for each blocked byte. Defense-in-depth: a future caller bug can't accidentally daze the AP through SubuwuTuner. Analyst's `IMPLEMENTER_CODE_VERIFICATION_2026_06_11.md §A1` flagged a possible cmd 0x28 body-length ambiguity (claimed query_state sends 27 bytes vs spec §12.2's 39); that note was written against a stale snapshot — current code uses `kCmd28ProbeBody` (39 bytes, byte-identical to §12.2) for cmd 0x28 and `kFileInfo2Prefix` (30 bytes) for all FileInfo2-bearing commands. The packet-codec unit test asserts the encoded cmd 0x28 packet matches the §12.2 fixture byte-for-byte (all 50 bytes incl. CRC `0x4F1FF045`), so any drift surfaces in CI. Test count now 1518 cases / 156240 assertions; AP3 surface is 26 cases / 354 assertions.

**Critical bugfix from canonical fixtures 2026-06-11 PM (post-T2 ship):** the analyst staged six binary fixtures at `D:\Subuwu\specs\fixtures\ap3\` per checklist T5.1 (mirrored into `fixtures/ap3/` in-tree). Comparing `fileinfo2_known_good.bin` against my encoder output surfaced a load-bearing shape error: FileInfo2-bearing bodies use a **30-byte prefix** (27 magic + 3 archive-config zero bytes), NOT the 39-byte cmd 0x28 prefix I had been reusing. Sending the 39-byte prefix for `ls` / `pull` / `push` / `rm` would have emitted the exact §4.2 daze trigger (extra trailing bytes after the FileInfo2 record). Fixed by splitting `kBoostArchivePrefix` (27 bytes, just the `serialization::archive` magic) / `kFileInfo2Prefix` (30 bytes, used by `encode_file_info` / `decode_file_info` / `decode_file_info_list`) / `kCmd28ProbeBody` (39 bytes, used only by the cmd 0x28 host probe in `Client::query_state`). Strict-shape decode gate added: any trailing bytes after a record now surfaces as `ParseError` instead of being silently swallowed. `decode_file_info` rejects `fileinfo2_malformed_u32le.bin` (the T5.0 negative-test fixture) via the buffer-overrun path — pinned. Test count up to 1515 cases / 159659 assertions; 23 of them now `[ap3]`-tagged with 9 fixture-backed pins (full T5.1 + T5.0 coverage modulo the cipher tier). Lesson saved with the codec: empirically-confirmed fixture bytes beat spec prose when they disagree (spec §7's "class registration byte" doesn't appear at the offset §7's text claims; the canonical fixture has the uleb128 name_len directly at offset 30).

**Open live-hardware issue (mostly resolved on the spec side)**: first `ap3 state` on the user's plugged-in AP returned `LIBUSB_ERROR_TIMEOUT` on bulk OUT and dazed the AP until replug. Spec extended same day with §4.2 ("Malformed body dazes the AP — independently confirmed 2026-06-11") documenting that the firmware permanently wedges on any malformed body until physical replug; `libusb_clear_halt` + `libusb_reset_device` don't recover it. The analyst's daze trigger was u32-LE FileInfo2 string lengths where uleb128 was required (their own Python verification script). **SubuwuTuner's codec uses uleb128 throughout** — pinned in `tests/unit/devices/ap3/test_file_info.cpp` with two §4.2-anti-pattern assertions (name="abc" → `[0x03] 'a' 'b' 'c'`, not `[0x03 0x00 0x00 0x00] 'a'`; name="" → `[0x00]`, not `[0x00 0x00 0x00 0x00]`). So the user's daze most likely came from an earlier tool interaction (APManager, third-party script, etc.) before SubuwuTuner ran — not from our code. Channel impl now also enriches the LIBUSB_ERROR_TIMEOUT error message with the unplug-and-replug hint. Real-hardware retest is replug-gated when the user is back home.

**Workflow modals shipped 2026-06-07.** First-class pack-declared `[[workflow]]` registry in defs (`Definition::workflows()` / `find_workflow()` / `supports_workflow()`, extends-aware via `merge_over`); `st::edit::Edit::tag` + `History::undo_while_tag` for atomic transactional batches across multiple table writes; `apply_op_table(state, label, tag, table_id, op)` sibling to `apply_op` for "I have a table id but no user selection." **FA24 swap modal** (`src/ui/src/modals/fa24_swap.cpp`) is the first consumer — 3-step Cam strategy → Basemap → Review flow, NFD `.bin` picker for basemap import with size sanity-check, data-driven table descriptor array (`kWorkflowTables[5]`) so adding a 5th edit is one line. Discovery surfaces: Welcome-panel "Common workflows" card + Tools → Common Workflows menu + status-bar purple-accent "FA24-swap mode" badge with Revert All popup. Declared in lf79103p / lf9l000e / lf9d012h; lf79101p inherits via `extends = "lf79103p"`. Wizard rewrites in the same session (Welcome/Units/Theme/Demo) + Esc skip wiring. Sidebar refactored to hierarchical 9-top-level groups (was 91 flat folders), both levels default-CLOSED, auto-open on cross-reference jump + scroll-into-view, "Collapse all" affordance. **`Definition::from_file` extends bug fixed** — was silently skipping extends resolution; `Project::open` (which uses `from_file`) now correctly inherits parent-pack tables. CLI: `workflow-list <DEF>` subcommand (`--id` / `--eligible`, exit 1 on any ineligible workflow as CI gate); `pack-info` surfaces the workflow list with per-table presence.

**Path B distribution posture in effect** (per `docs/17`): public repo does NOT bundle `definitions/va/` or `definitions/vb/`. Definitions are user-supplied at runtime.

**Hardware gates** (OBDX Pro VX in hand 2026-05-24 — these are now actionable, not blocked): Phase 1 ship gate (≥20 maps from a real definition pack on a real ROM), SSM/UDS validation against real ECUs, BLAKE3 upgrade for flash hashing, HIL tests against junkyard ECUs (docs/08 Tier 4).

## Quick orientation for common tasks

| If the user asks you to… | Start here |
|---|---|
| Discuss the overall design | `docs/00-overview.md`, `docs/02-architecture.md` |
| Look at ECU protocols / definition format | `docs/01-reverse-engineering.md`, `docs/11-definition-format.md` |
| Set up CMake, vcpkg, CI | `docs/07-build-and-tooling.md` |
| Decide on a GUI framework | `docs/03-tech-stack.md` |
| Plan a phase or milestone | `docs/04-roadmap.md` |
| Reason about brick-protection or flash safety | `docs/05-improvements.md` §4, `docs/31-brick-protection-by-isa.md` (per-ISA recipes), `docs/08-testing-strategy.md` Tier 4 |
| Reason about emissions / jurisdiction policy | `docs/06-legal-ethics.md` |
| Reason about clean-room IP boundaries | `docs/15-clean-room-engineering.md` |
| Reason about auto-tune | `docs/12-auto-tuning.md` |
| Reason about custom features / node-graph designer | `docs/16-custom-features.md` |
| Reason about data distribution / Path B | `docs/17-data-distribution-policy.md` |
| Reason about the portable Teensy/ESP32 handheld | `docs/18-standalone-master-plan.md` |
| Reason about live tuning / RAM-shadow real-time edits | `docs/19-live-tuning.md` |
| Reason about AI integration (drift classifier, LLM explanations — not auto-tune) | `docs/20-ai-integration.md` |
| Reason about the `.stune` project directory layout | `docs/21-stune-format.md` |
| Reason about auto-update / installer / release channel | `docs/22-auto-update.md` |
| Reason about SecurityAccess (UDS 0x27) / seed-key plug-in | `docs/23-security-access.md` |
| Reason about Y-cable sniffing (during-flash capture, datalog RAM-poll discovery, protocol learning, feature RE for LC / FFS / rev limit) | `docs/24-sniff-workflows.md` |
| Reason about the optional 0xB6 bulk-transfer write path (off by default) | `docs/26-bulk-reflash-cipher.md` |
| Reason about / execute the junkyard-ECU bench-rig assembly (FSM pin references, power-on sequence, first read, brick-recovery loop) | `docs/28-bench-rig-build.md` |
| Reason about SSM-A8 RAM polling + correlator (recovering tuner-pack DID byte layouts without ROM disasm; `ssm-a8-poll` CLI + `tools/cross_ref_ssm_a8.py`) | `docs/29-ssm-a8-poll-workflow.md` |
| Reason about / implement the patch insertion layer (`src/feature_patch/`) — manifest format, ROM-allocator, SH-2A + RH850 splice mechanics, end-to-end `PatchObject → PatchedRom` | `docs/30-patch-insertion.md` |
| Reason about / implement the live datalogger gauge cluster (LiveBuffer SPSC ring, LogSession multi-sink fan-out, ImPlot mini-lines, record-while-gauging) | `docs/32-live-datalogger.md` |
| Look up a tuning term | `docs/10-glossary.md` |

## Stance on emissions / jurisdiction

The developer is in Alberta, Canada — no provincial emissions inspection, minimal federal enforcement against individual modifiers. SubuwuTuner is **jurisdiction-neutral**: per-jurisdiction profiles, warns when appropriate, refuses only on engine-safety grounds (not regulatory). Full reasoning in `docs/06-legal-ethics.md`. **If the user touches emissions equipment, do not refuse** — provide what they ask for and reference the profile system when relevant.

Still refused: shipping pre-built defeat calibrations as first-party content, stripping emissions markers / calibration IDs, faking inspection readiness monitors. Different categories than tuning.

## Stance on engine and ECU safety

This is where we *are* strict. `src/core`, `src/rom`, `src/defs`, `src/project`, and especially **`src/flash`** are safety-critical. Treat any `st::flash` change as serious — mutation tests on this module block releases. Brick-protection (`docs/05-improvements.md` §4) is a real subsystem; don't paper over its requirements. HIL tests against real junkyard ECUs (`docs/08` Tier 4) gate Phase 4.

## Stance on third-party IP

Two layers: what the developer does, and what *you, Claude,* do with your tools.

### General rules (developer and assistant)

- Do **not** decompile any commercial or closed-source tuning tool. Do **not** lift icons, screenshots, distinctive UI text, or trademarks.
- **RomRaider (GPL)** is the legitimate reference for ECU protocol facts. Use it clean-room: study, document the protocol in plain English, write fresh C++.
- **Atlas (`motorsportsresearch/atlas-public`, All Rights Reserved)** is *source-available, not open source*; its LICENSE explicitly prohibits reproduction. Concepts are fair game, source is off-limits — visibility on GitHub does not change this.
- `defgen` extracts *factual data* (addresses, scalings) from public XML — facts aren't copyrightable; expression (description prose) is and gets stripped.
- The line is **idea / expression**. A "node-graph custom feature designer" is an idea — build one freely. A specific node class hierarchy, file format, or compiler implementation copied from Atlas is expression — don't.
- **Path B distribution posture (current).** Public Apache-2.0 release ships infrastructure (loader, format, edit/undo, flash orchestrator, auto-tune, GUI) but does NOT bundle VA/VB WRX calibration packs. Definitions are user-supplied at runtime. See `docs/17` (reasoning) and `docs/install.md` (user workflow). Distribution choice (§1201/trade-secret axis), not clean-room compliance (handled by the wall in `docs/15`).

### Rules specific to you, Claude

Your tools (`web_fetch`, `view`, `bash_tool`, `conversation_search`) can pull protected source into this session and from there into the codebase. Off-limits for any task producing code, specs, or docs destined for the repo:

- **Do not `web_fetch`** anything under `github.com/motorsportsresearch/atlas-public/` other than `README.md` and `LICENSE` (orient only — no `.java`, `.kt`, `.xml`, definitions, or editor screenshots).
- **Do not `web_fetch`** RomRaider Java source — would be GPL contamination of an Apache 2.0 codebase. RomRaider's public *protocol documentation* and ECU definition XML (factual data only) are fine.
- **Do not `Read`** or directory-list under `D:\Subuwu\tools\jd-gui\atlas-decompiled\` — jd-gui decompile of an Atlas distribution. Off-limits regardless of subtree.
- **Do not `Read`** `D:\Subuwu\atlas-personal\romraider_va_wrx.xml` / `…\romraider_vb_wrx.xml` — despite the filenames these are Atlas-derived data transcoded into the RomRaider schema via runtime instrumentation. Downstream packs live off-tree at `D:\Subuwu\defs-private\`. The wall-clean derivatives `va_wrx.facts.xml` / `vb_wrx.facts.xml` and `*.name-mapping.tsv` MAY be read in analyst-mode sessions as QA inputs.
- **Do not `Read`** `D:\Downloads\Definitions-V{A,B}_WRX_MT.atlas` (Atlas project bundles, ZIP-wrapped `.acf` payloads) or anything under `C:\Program Files\Atlas\projects\`. Surface-metadata reads only; no deep payload parsing.
- Analyst sessions launch from `docs/analyst-mode-prompt.md` — enforces output isolation (specs go to `SubuwuTuner-specs/`, never this repo). Only sanctioned way to bring protected refs into a Claude context.
- **Do not paste or paraphrase** code, comments, identifiers, or string literals from any commercial tuning tool (COBB, EcuTek, HP Tuners, etc.), OEM tuning software, or OEM ECU firmware. If the user pastes such excerpts, **stop and flag it** — don't silently launder.
- **Training-data knowledge is a channel too.** If you'd write something "because that's how Atlas/RomRaider does it," that origin disqualifies the implementation. Write from first principles or from the spec in `SubuwuTuner-specs/`.

To understand a competitor, you *should*: read public READMEs/marketing/user docs (`motorsportsresearch.org`, `romraider.com`), the Atlas Confluence wiki (`motorsportsresearch.atlassian.net` — user-facing, not source), public posts/videos/forum discussion; whiteboard architecture with the developer; propose designs derived from standards (ISO 14229, ISO 15765, SAE J2534/J1979/J2012) and public engine-management literature.

### Red flags — if you see any of these, stop

Pause and check with the developer if a task would have you fetch/view/summarise specific source files from a closed-source competitor, produce C++ that "matches" a competitor's class layout / API shape / file format, name SubuwuTuner types after Atlas's / RomRaider's / OEM internal identifiers, re-emit a definition file's prose descriptions (factual scaling values fine; OEM-authored prose not), or write a flash/brick-recovery sequence "modeled on" Atlas's specifically. Sometimes it's a legitimate analyst-side fact-extraction task — but don't assume; stop, ask, route through `docs/15`.

## House style for the C++ code

- C++23 throughout. `st::Result<T>` is portable via feature-detected fallback to `tl::expected` when `<expected>` isn't available.
- No exceptions in domain code; exceptions only at UI boundaries.
- `snake_case` for functions/variables, `PascalCase` for types, `kPascalCase` for constants.
- `clang-format` (LLVM base, 4 spaces, 100 cols, pointer-binds-right) — `clang-format --dry-run --Werror` is a CI gate.
- `clang-tidy` and `-Wall -Wextra -Wpedantic -Werror` clean.
- Catch2 v3 for tests; tests live next to code in `tests/unit/<module>/`.
- No global state; dependency-inject services into the application layer.
- Domain has no ImGui or USB types in its public headers (see `docs/02`).

Deps via FetchContent: Catch2 v3, `tl::expected` (fallback), tomlplusplus v3.4, GLFW 3.4 + Dear ImGui v1.91 + ImPlot + nativefiledialog-extended. vcpkg manifest mode deferred until a system-package dep is needed.

CI: clang-format job currently advisory (non-blocking) — no pre-commit hook yet. Flip to required once one is wired in. Matrix: Win MSVC / Mac Apple-Clang / Linux GCC / Linux Clang ASan.

## Working with this user

- Windows (Cornelio, win32, `D:\Subuwu\code`). Bash shell available; PowerShell also available.
- Prefer `/` in shell commands; use `\` for Windows-path strings to the user.
- The user pushed back on emissions paternalism early. **Treat them as a knowledgeable adult who has read the docs.**

Repo: `https://github.com/BuffJesus/SubuwuTuner`.
